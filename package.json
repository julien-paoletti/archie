{
  "name": "archie",
  "version": "0.7.2",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun build src/app.ts --outdir=docs --watch",
    "build": "bun run scripts/build.ts",
    "build:viewer": "bun run scripts/build-viewer.ts",
    "serve": "bun --bun run scripts/serve.ts",
    "patch": "bun run scripts/patch-version.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@julien-paoletti/archie-viewer": "file:viewer"
  }
}